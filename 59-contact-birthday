public Boolean isBirthday(Contact c) {
    Date todaysDate = System.today();
    Date birthday = c.Birthdate;

    if (todaysDate.month() == birthday.month() && todaysDate.day() == birthday.day()) {
        return true;
    }

    return false;
}