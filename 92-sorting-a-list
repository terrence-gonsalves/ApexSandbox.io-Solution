public List<String> getNamesInAscOrder(List<String> accountNames)
{
    if (accountNames == null || accountNames.size() == 0) {
        return null;
    }

    List<String> names = new List<String>(accountNames);

    return names.sort();
}
