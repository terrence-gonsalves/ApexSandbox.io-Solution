public static List<Integer> mergeLists(List<Integer> list1, List<Integer> list2){
    List<Integer> merged = new List<Integer>();

    if (list1.size() == 0 && list2.size() == 0) return merged;

    merged.addAll(list1);
    merged.addAll(list2);

    merged.sort();
    
    return merged;
}